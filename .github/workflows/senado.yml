name: Actualización de datos del Senado

on:
#  schedule:
#    - cron: '10 21 * * *' # Actualiza a las 21:10 UTC (18:10 ART)
  workflow_dispatch:

jobs:
  update:
    uses: ./.github/workflows/reusable-data-update.yml
    with:
      run_command: pnpm run senado:test:run
      needs_build: false
      env_preset: minimal
    secrets: inherit
